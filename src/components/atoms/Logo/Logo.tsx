import React from 'react';
import styled from 'styled-components';

const LogoWrapper = styled.div`
  font-size: 24px;
  font-weight: bold;
`;

export const Logo = () => <LogoWrapper>Logo</LogoWrapper>;